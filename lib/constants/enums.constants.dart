/// Enum to identify the current active view.
enum AppSections {
  homeMain,
  homeSearch,
}

/// Enum to identify the differente spotify images sizes.
enum SpotifyImageSizes {
  unknow,
  big,
  midle,
  small,
}
